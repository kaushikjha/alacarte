<h1>Editing item</h1>

<% form_for(@item) do |f| %>
  <%= render "form", :f => f %>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to "delete #{@item.name}", @item, :method => :delete, :confirm => "Are you sure you want to delete #{@item.name}?" %>